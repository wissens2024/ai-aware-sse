{
  "name": "ai-aware-sse-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs --mode=production",
    "build:dev": "node build.mjs --mode=development",
    "build:prod": "node build.mjs --mode=production",
    "watch": "node build.mjs --mode=development --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "esbuild": "^0.24.0",
    "typescript": "^5.0.0"
  }
}
